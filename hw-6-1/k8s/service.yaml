apiVersion: v1
kind: Service
metadata:
  name: llm-gateway
  namespace: app
  labels:
    app: llm-gateway
spec:
  selector:
    app: llm-gateway
  ports:
    - name: http
      port: 8000
      targetPort: http
